* Portugues Brasil
# Defendi Studio

## Plataformas obrigatórias Responsivo:
- Desktop (PC)
- Firest Mobile (Telas menores)

## tecnologias:
- HTML
- CSS
- JS

## SEO
- `index.html` usa semântica HTML5 (`main`, `section`, `article`, `header`, `footer`), hierarquia de headings estruturada (h1 → h2 → h3), meta description, Open Graph, Twitter Card e Schema Markup (JSON-LD, tipo `BeautySalon`).
- `robots.txt` e `sitemap.xml` na raiz do projeto, apontando para `https://studio.defendipiercing.com.br/`.